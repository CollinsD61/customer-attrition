[tool.pytest.ini_options]
addopts = "--import-mode=importlib"

[tool.ruff]
target-version = "py311"
exclude = [".opencode", "notebooks", ".gitnexus"]

[tool.mypy]
python_version = "3.14"
ignore_missing_imports = true
warn_unused_ignores = false
files = ["backend", "mlops/src", "serving"]
